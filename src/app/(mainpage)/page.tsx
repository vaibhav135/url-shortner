'use client';

import { HomePage } from '@/components/homepage';

const MainPage = () => {
    return (
        <div className="w-full h-full">
            <HomePage />
        </div>
    );
};

export default MainPage;
